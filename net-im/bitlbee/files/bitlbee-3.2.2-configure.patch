--- bitlbee-3.2.2/configure
+++ bitlbee-3.2.2/configure
@@ -628,14 +628,6 @@
 	fi
 fi
 
-case "$CC" in
-*gcc* )
-	echo CFLAGS+=-MMD -MF .depend/\$@.d >> Makefile.settings
-	for i in . lib tests protocols protocols/*/; do
-		mkdir -p $i/.depend
-	done
-esac
-
 if [ "$msn" = 0 ]; then
 	echo '#undef WITH_MSN' >> config.h
 else
