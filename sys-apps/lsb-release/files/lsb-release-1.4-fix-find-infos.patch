--- lsb_release.orig	2000-12-15 02:19:16.000000000 -0500
+++ lsb_release	2014-01-15 18:34:21.680100838 -0500
@@ -250,9 +250,10 @@
             CHECKFIRST=$(find $INFO_ROOT/ -maxdepth 1                         \
                    -name \*$INFO_DISTRIB_SUFFIX                               \
                    -and ! -name $INFO_LSB_FILE                                \
+                   -and ! -name os-release                                    \
                    -and -type f                                               \
                    2>/dev/null                                                \
-                   | head -1 ) # keep one of the files found (if many)
+                   | sort | head -1 ) # keep one of the files found (if many)
         fi
         InitDistribInfo $CHECKFIRST
     fi
